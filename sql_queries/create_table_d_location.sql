CREATE TABLE "monthlysales"."d_location" (
    postal_code    VARCHAR(100) PRIMARY KEY,
    city           VARCHAR(200),
    state        VARCHAR(100),
    country      VARCHAR(100),
    region       VARCHAR(100) 
);